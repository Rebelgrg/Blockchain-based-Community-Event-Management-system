/**
 * Address of the first contract deployed from Hardhat account #0 on a fresh
 * `npx hardhat node` chain (nonce 0). Matches `web/src/lib/contractAddress.ts`.
 * After extra deployments, override with `EVENT_MANAGEMENT_ADDRESS` / env files.
 */
export const LOCAL_EVENT_MANAGEMENT_ADDRESS =
  "0x5fbdb2315678afecb367f032d93f642f64180aa3" as const;
